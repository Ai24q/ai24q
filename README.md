# 🤖 Ai24q Website Template

Сучасний, блискавично швидкий сайт-лендинг та блог, створений на чистому HTML, GitBook та Cloudflare Pages.

**Особливості:**
- Дизайн преміум-класу з градієнтами та glassmorphism.
- 100/100 у Google PageSpeed Insights.
- SEO-оптимізований "з коробки".
- Блог керується через зручний редактор GitBook.
- Все на одному домені.
- Повністю безкоштовно.

---

## 🚀 Швидкий старт (3 кроки)

### 1. Форкніть цей репозиторій
Натисніть кнопку `Use this template` або `Fork` у верхньому правому куті.

### 2. Підключіть GitBook
1.  Створіть новий простір (Space) на [GitBook](https://gitbook.com).
2.  Перейдіть в `Integrations` → `Git Sync`.
3.  Налаштуйте синхронізацію:
    - **Repository:** `ai24q` (або як ви його назвали)
    - **Branch:** `main`
    - **Project directory:** `blog`
    - **Initial sync:** `GitBook to GitHub`
4.  Натисніть `Sync`. Тепер усі статті, написані в GitBook, автоматично з'являтимуться у папці `blog` у вашому репозиторії.

### 3. Розгорніть на Cloudflare Pages
1.  Зайдіть в [Cloudflare Pages](https://pages.cloudflare.com/).
2.  Натисніть `Create a project` → `Connect to Git`.
3.  Виберіть GitHub і знайдіть ваш репозиторій `ai24q`.
4.  Налаштування збірки:
    - **Build command:** ` (залиште порожнім)
    - **Build output directory:** `/` (корінь)
5.  Натисніть `Save and Deploy`.

**Вітаю!** Ваш сайт живий за адресою `ваша-назва.pages.dev`.

---

## 📝 Як писати статті для блогу?

1.  Заходьте у ваш простір на GitBook.
2.  Натискаєте `+ Add page`.
3.  Написуєте статтю (підтримується Markdown).
4.  Зберігаєте. Через хвилину стаття автоматично з'явиться на вашому сайті за адресою `https://ваша-назва.pages.dev/blog/назва-статті`.

---

## 🎨 Як змінити дизайн блогу (GitBook)?

Щоб сторінки блогу виглядали так само стильно, як і головна:

1.  У GitBook перейдіть до вашого простору.
2.  Натисніть `...` (меню) → `Share` → `Customization`.
3.  У вкладці `Custom CSS` вставте цей код:

```css
:root {
  --bg: #0f0f1a;
  --text: #e2e8f0;
  --accent: #00d4ff;
  --card: #1a1a2e;
}

body {
  font-family: 'Inter', sans-serif;
  background: var(--bg);
  color: var(--text);
}

h1, h2, h3, h4, h5, h6 {
  background: linear-gradient(135deg, #00d4ff, #7c3aed);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  font-weight: 700;
}

a {
  color: var(--accent);
  text-decoration: none;
  transition: color 0.3s;
}

a:hover {
  text-decoration: underline;
}

/* Можна додати стилі для цитат, коду тощо */
blockquote {
  border-left: 3px solid var(--accent);
  padding-left: 1rem;
  margin: 1.5rem 0;
  color: #94a3b8;
}
